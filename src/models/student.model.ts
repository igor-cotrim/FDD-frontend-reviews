export type studentSemesterModel = {
    semester: string;
    date: string;
    current: boolean;
};

export type testStudentModel = {
    id: number;
    question: string;
    registrationNumber: number;
};
