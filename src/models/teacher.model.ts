export type teacherSemesterModel = {
  semester: string;
  date: string;
  current: boolean;
};

export type teacherDisciplineModel = {
  discipline: string;
};

export type teacherStudentModel = {
  name: string;
  code: number;
};
